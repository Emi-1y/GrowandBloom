services:
  app:
    build: .
    ports:
      - "80:80"
    environment:
      APP_NAME: GrowandBloom
      APP_ENV: production
      APP_DEBUG: false
      APP_URL: http://136.112.218.174
      DB_CONNECTION: mysql
      DB_HOST: 35.193.116.102
      DB_PORT: 3306
      DB_DATABASE: growandbloomdb
      DB_USERNAME: root
      DB_PASSWORD: jHP}`c5n=HT,@z[&
      SESSION_DRIVER: database
      CACHE_STORE: database
      QUEUE_CONNECTION: database
    volumes:
      - storage:/var/www/html/storage

volumes:
  storage:
